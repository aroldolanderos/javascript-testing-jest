const sumar = (a, b) => {
    return a + b;
};

const multiplicar = (a, b) => {
    return a * b;
};

const restar = (a, b) => {
    return a - b;
};

const dividir = (a, b) => {
    return a / b;
};

export default {
    sumar,
    multiplicar,
    restar,
    dividir
}
